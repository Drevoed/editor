import { LocalTransforms } from '../transforms';
export const outdent = ({ editor, event }) => {
    event.preventDefault();
    LocalTransforms.outdent(editor);
};
//# sourceMappingURL=outdent.js.map